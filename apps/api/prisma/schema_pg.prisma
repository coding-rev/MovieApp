generator client {
  provider = "prisma-client-js"
   output   = "./prisma_pg"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Movie {
  id        String @id @default(cuid())
  title     String
  year      Int
  genre     String
  rating    Float
  thumbnail String?
}
