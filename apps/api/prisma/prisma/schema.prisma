generator client {
  provider = "prisma-client-js"
  output   = "prisma"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Movie {
  id        String  @id @default(cuid())
  title     String
  year      Int
  genre     String
  rating    Float
  thumbnail String?
}
